/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Enable static export mode
  basePath: "/Website.github.io", // Replace with your GitHub repo name
  // distDir: 'dist',  // Optional: You can specify a custom output directory
};

export default nextConfig;
